let tableau = [4, 7, 1, 23, 24, 25];

for (let i = 0; i < tableau.length; i++) {
  if (tableau[i] % 2 == 0) {
    console.log(tableau[i] + " ce nombre est pair");
  } else {
    console.log(tableau[i] + " ce nombre est impair");
  }
}
